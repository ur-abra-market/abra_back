version: "3.9"

services:
  tests:
    image: tests:latest
    build:
      context: .
      dockerfile: Dockerfile.tests
    volumes:
      - ./:/app
    env_file:
      - .env
    environment:
      - PYTHONPATH=app
      - RDS_HOSTNAME=tests_database
    depends_on:
      - tests_database
